import { useEffect, useState } from 'react';
import { localDb } from '../../lib/localDb';

type Item = { id: string; title: string; image_data: string; details: string; source_type: string; created_at: string; customer_name: string };
export default function PublicGallery({ slug }: { slug: string }) { const [items, setItems] = useState<Item[]>([]); useEffect(() => { void localDb.getPublicStoreGallery(slug).then(setItems).catch(() => setItems([])); }, [slug]); if (!items.length) return null; return <section className="app-theme mt-8 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-bold text-slate-900">Customer candle gallery</h2><p className="mt-1 text-sm text-slate-600">Approved custom candles and collections shared by our customers.</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map((item) => <article key={item.id} className="overflow-hidden rounded-xl border border-slate-200 bg-slate-50">{item.image_data ? <img src={item.image_data} alt={item.title} className="h-40 w-full object-cover" /> : <div className="flex h-40 items-center justify-center bg-pink-100 text-sm font-semibold text-pink-800">{item.source_type === 'collection' ? 'Candle collection' : 'Custom candle'}</div>}<div className="p-3"><p className="font-semibold text-slate-900">{item.title}</p>{item.details ? <p className="mt-1 line-clamp-2 text-xs text-slate-600">{item.details}</p> : null}<p className="mt-2 text-xs text-slate-500">Shared by {item.customer_name}</p></div></article>)}</div></section>; }

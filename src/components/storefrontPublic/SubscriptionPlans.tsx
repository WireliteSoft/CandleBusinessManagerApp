import { useEffect, useState } from 'react';
import { localDb } from '../../lib/localDb';
type Plan = { id: string; name: string; description: string; candle_count: number; monthly_price: number; quarterly_price: number };
export default function SubscriptionPlans({ slug }: { slug: string }) { const [plans, setPlans] = useState<Plan[]>([]); useEffect(() => { void localDb.getPublicSubscriptionPlans(slug).then(setPlans).catch(() => setPlans([])); }, [slug]); if (!plans.length) return null; return <section className="app-theme mt-8 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-bold text-slate-900">Candle subscriptions</h2><p className="mt-1 text-sm text-slate-600">Choose a plan today. Subscription enrollment, billing, and delivery controls will be available once the store activates recurring checkout.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{plans.map((plan) => <article key={plan.id} className="rounded-xl border border-slate-200 bg-slate-50 p-4"><h3 className="font-bold text-slate-900">{plan.name}</h3><p className="mt-1 text-sm text-slate-600">{plan.description}</p><p className="mt-3 text-sm">{plan.candle_count} candle{plan.candle_count === 1 ? '' : 's'} per delivery</p><p className="mt-2 font-semibold text-slate-900">${Number(plan.monthly_price).toFixed(2)} monthly</p><p className="text-sm text-slate-700">${Number(plan.quarterly_price).toFixed(2)} quarterly</p></article>)}</div></section>; }

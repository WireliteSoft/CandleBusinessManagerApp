import { useState } from "react";
import type { StorefrontProductSummary } from "../../lib/localDb";
const discounts: Record<number, number> = { 3: 20, 6: 40, 12: 60 };
export default function MysteryBoxModal({ products, onClose, onAdd }: { products: StorefrontProductSummary[]; onClose: () => void; onAdd: (id: string) => void }) { const [size, setSize] = useState(3); const eligible = products.filter((product) => product.quantity_in_stock > 0 && product.product_type !== "sample"); const canBuild = eligible.length >= size; const addBox = () => { const picks = [...eligible].sort(() => Math.random() - 0.5).slice(0, size); picks.forEach((product) => onAdd(product.id)); onClose(); }; return <div className="app-theme fixed inset-0 z-[80] overflow-y-auto bg-slate-950/60 p-4"><section className="store-customer-modal mx-auto my-8 w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between gap-4"><div><h2 className="text-xl font-bold text-gray-900">Mystery candle box</h2><p className="mt-1 text-sm text-gray-600">We choose surprise scents from the current in-stock candle selection.</p></div><button type="button" onClick={onClose} className="text-sm font-semibold text-pink-700">Close</button></div><div className="mt-5 grid grid-cols-3 gap-2">{[3, 6, 12].map((count) => <button type="button" key={count} onClick={() => setSize(count)} className={`rounded-lg border px-3 py-3 text-sm font-semibold ${size === count ? "bg-pink-600 text-white" : "border-gray-300 text-gray-700"}`}>{count} candles<br /><span className="text-xs">{discounts[count]}% off</span></button>)}</div><p className="mt-5 rounded-lg bg-gray-50 p-3 text-sm text-gray-700">The exact products remain a surprise until your box is added. Your {discounts[size]}% quantity discount is verified again during checkout.</p><button type="button" disabled={!canBuild} onClick={addBox} className="mt-5 w-full rounded-lg bg-pink-600 px-4 py-3 font-semibold text-white disabled:opacity-50">{canBuild ? `Add ${size}-candle mystery box` : `Not enough in-stock candles for ${size}-candle box`}</button></section></div>; }

import type { StorefrontProductSummary } from "../../lib/localDb";
const packs = [{ name: "Fresh & Clean", terms: ["fresh", "clean"] }, { name: "Sweet & Bakery", terms: ["sweet", "bakery"] }, { name: "Floral", terms: ["floral"] }, { name: "Wood & Masculine", terms: ["wood", "masculine"] }, { name: "Fruit", terms: ["fruit", "fruity"] }, { name: "Relaxation", terms: ["relax", "lavender"] }, { name: "Seasonal", terms: ["seasonal", "holiday"] }];
export default function ScentPortfolioModal({ products, onClose, onAdd }: { products: StorefrontProductSummary[]; onClose: () => void; onAdd: (id: string) => void }) { const matches = (terms: string[]) => products.filter((product) => product.quantity_in_stock > 0 && terms.some((term) => `${product.name} ${product.scent_family} ${product.fragrance_notes} ${product.mood} ${product.season}`.toLowerCase().includes(term))).slice(0, 3); return <div className="app-theme fixed inset-0 z-[80] overflow-y-auto bg-slate-950/60 p-4"><section className="store-customer-modal mx-auto my-8 w-full max-w-3xl rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between gap-4"><div><h2 className="text-xl font-bold text-gray-900">Scent portfolio packs</h2><p className="mt-1 text-sm text-gray-600">Curated packs based on this store's available scent metadata.</p></div><button type="button" onClick={onClose} className="text-sm font-semibold text-pink-700">Close</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{packs.map((pack) => { const items = matches(pack.terms); return <div key={pack.name} className="rounded-xl border border-gray-200 p-4"><h3 className="font-bold text-gray-900">{pack.name}</h3>{items.length ? <><p className="mt-2 text-sm text-gray-600">{items.map((item) => item.name).join(", ")}</p><button type="button" onClick={() => { items.forEach((item) => onAdd(item.id)); onClose(); }} className="mt-3 rounded-lg bg-pink-600 px-3 py-2 text-sm font-semibold text-white">Add pack to cart</button></> : <p className="mt-2 text-sm text-gray-500">No matching in-stock products yet.</p>}</div>; })}</div></section></div>; }
